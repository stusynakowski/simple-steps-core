"""A minimal simple-steps server.

Define a few tools, and you get a running API that lists them, calls them, and
runs multi-step workflows (with inline orchestration) that manage the session
for you. See ``README.md`` to run it.
"""
